@model MovieShop.Core.Models.MovieResponseModel

<div class="card mb-4 shadow bg-light">
    @* <a asp-controller="Movies" asp-action="Details" asp-route-id="@Model.Id"> *@
    <a>
        <img src="@Model.PosterUrl" onerror="this.src='https://picsum.photos/id/237/200/300'" class="card-img-top img-fluid" alt="@Model.Title">
        <div class="card-body">
            <h5 class="card-title">@Model.Title</h5>
        </div>
    </a>
</div>